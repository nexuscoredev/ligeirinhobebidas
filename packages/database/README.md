# @ligeirinho/database

Schema PostgreSQL (Prisma) do ecossistema.

```bash
cp .env.example .env
# configurar DATABASE_URL

npm install
npm run generate
npm run migrate:dev
npm run seed
```

Ver `docs/database/modelagem-inicial.md`.
